"""
Implement a Digit Iterator 

This iterator iterates over an integer from left to right

p = DigitIterator(1823)
print(next(p)) # 1
print(next(p)) # 8
print(next(p)) # 2
print(next(p)) # 3
print(next(p)) # StopIteration

"""

# https://repl.it/Br0r/0
# Here's a solution, I just realized its backwards from what
# you have. But I think that's how it was on the OOP exam
# (least sig digit to most). Ima do another solution
# the other way around too though.
